module.exports = function() {

//    this.Then(/^I should see a ([^']+) h1 tag$/, function (arg1, callback) {
//        this.expect(this.page.headerLevelOne.getCssValue('font-size')).to.eventually.equal(arg1).and.notify(callback);
//    });
};
